{
  "buildCommand": "cd frontend && npm install --legacy-peer-deps && node node_modules/vite/bin/vite.js build",
  "outputDirectory": "frontend/dist",
  "routes": [
    {
      "src": "/api/(.*)",
      "destination": "https://kirana-store-api.onrender.com/api/$1"
    },
    {
      "src": "/(.*)",
      "destination": "/index.html"
    }
  ]
}